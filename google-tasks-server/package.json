{
  "name": "ink2task-google-tasks-server",
  "version": "0.1.0",
  "private": true,
  "description": "Google Tasks backend for the Ink2Task Supernote plugin -- a drop-in alternative to mac-server, runnable on any always-on host.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "authorize": "tsx src/authorize.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
